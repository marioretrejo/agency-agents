# Port the signaling server listens on
PORT=5000

# Allowed CORS origin(s). Use * for development only.
CORS_ORIGIN=*

# How long an empty room is kept alive, in milliseconds (default 1 hour)
ROOM_TTL_MS=3600000
